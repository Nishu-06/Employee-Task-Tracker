# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/taskflow_db

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskflow_db?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS Configuration
# Comma-separated list of allowed origins
# For production: include your Vercel frontend URL
# For local development: localhost is automatically added
CORS_ORIGIN=https://employee-task-tracker-one.vercel.app,http://localhost:5173
